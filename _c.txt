── components/GradeDialog.tsx:9
      7|  * 한 문항씩 크게 띄워 손으로 채점하는 창.
      8|  *
      9|  * 아래 표에서도 채점할 수 있지만 30문항을 훑으려면 스크롤을 해야 한다.
     10|  * 여기서는 O / X 를 누르면 바로 다음 문항으로 넘어가므로, 답안지를 보며
── components/GradeDialog.tsx:11
      9|  * 아래 표에서도 채점할 수 있지만 30문항을 훑으려면 스크롤을 해야 한다.
     10|  * 여기서는 O / X 를 누르면 바로 다음 문항으로 넘어가므로, 답안지를 보며
     11|  * 같은 자리에서 손만 움직이면 된다. 키보드로도 같다 — O·X·숫자·Enter.
     12|  */
── components/GradingPanel.tsx:84
     82|     });
     83|   // 팝업에서 쓰는 setter. 표의 setCell은 같은 값을 다시 누르면 지우는 토글이지만
     84|   // 여기서는 누른 값을 그대로 넣는다(넘어간 뒤 되돌아와도 값이 흔들리지 않게).
     85|   const putCell = (no: number, v: Cell) => setCells((c) => ({ ...c, [no]: v }));
── components/GradingPanel.tsx:120
    118|   };
    119| 
    120|   /** 불러온 채점 결과를 지운다. 화면의 입력칸도 같이 비워 되살아나지 않게 한다. */
    121|   const removeSaved = () => {
── components/ManualPage.tsx:190
    188|               <td>선택</td>
    189|               <td>
    190|                 같은 <code>papers/</code> 폴더의 파일 이름. 시험지 한 장에 하나씩이라 <b>첫 줄에만</b> 적으면 됩니다.
    191|               </td>
── components/ManualPage.tsx:353
    351|           <li>
    352|             <b>[직접 입력]</b> — 한 문항을 크게 띄웁니다. O나 X를 누르면 바로 다음 문항으로 넘어가므로, 답안지를 보며
    353|             같은 자리에서 손만 움직이면 됩니다. 키보드로도 같습니다 — <code>O</code> <code>X</code>(한글 자판{' '}
    354|             <code>ㅐ</code> <code>ㅌ</code>도) 또는 <code>1</code> <code>0</code>, <code>←</code> <code>→</code>{' '}
── components/ManualPage.tsx:411
    409|           <li>응시가 여러 번이면 리포트에 넣을 시험을 고르거나 기간으로 선택할 수 있습니다.</li>
    410|           <li>
    411|             <b>1쪽</b>은 유형 레이더 한 장입니다. 여덟 유형의 정답률을 도형으로 보여주고, 각 꼭짓점에{' '}
    412|             <code>25% 약점</code>처럼 글자를 함께 적어 흑백으로 인쇄해도 읽힙니다. 난이도는 제목 줄 오른쪽에{' '}
── components/PaperPicker.tsx:9
      7|  *
      8|  * 사이트의 papers/ 에 올려 둔 파일만 나온다. 시험지에는 파일 이름만 저장되므로
      9|  * 기존 동기화에 그대로 얹히고, 다른 기기에서도 그대로 열린다.
     10|  * 새 파일은 app/public/papers/ 에 넣고 push 하면 다음 배포부터 여기에 보인다.
── components/TypeRadar.tsx:100
     98|               <title>{s.type}</title>
     99|             </text>
    100|             {/* 막대를 걷어냈으므로 강점/약점을 색으로만 알리게 된다.
    101|                 흑백 인쇄와 색각 이상에서도 읽히도록 글자 라벨을 % 옆에 붙인다. */}
── components/TypeReport.tsx:23
     21| 
     22| /**
     23|  * 손으로 적을 날짜 칸. '20    년    월    일'처럼 공백을 늘어놓으면
     24|  * HTML이 공백을 하나로 합쳐 버려서 인쇄했을 때 적을 자리가 남지 않는다.
── components/TypeReport.tsx:128
    126|   const movedRef = useRef<HTMLDivElement>(null);
    127|   const analysisRef = useRef<HTMLElement>(null);
    128|   // 응시 이력이 길어지면 의견 두 칸이 1쪽에서 밀려난다. 그때만 쪽을 하나 더 낸다.
    129|   const [splitNotes, setSplitNotes] = useState(false);
── components/TypeReport.tsx:196
    194|    *
    195|    * 나뉘고 나면 1쪽은 분석 전용이 되어 레이더가 커지고 막대가 붙는다.
    196|    * 그 상태를 그대로 재면 "합치면 들어가는가"를 영영 알 수 없으므로,
    197|    * 유형 섹션을 좁은 배치였을 때의 높이로 되돌려 놓고 잰다.
── components/TypeReport.tsx:533
    531|                 <div className="rp-sec-row">
    532|                   <span className="report-sec-h">유형별 강점과 약점</span>
    533|                   {/* 난이도는 자리를 더 쓰지 않도록 이 머리줄에 얹는다.
    534|                       A4 한 쪽을 넘기지 않으려면 여기가 유일하게 남는 가로 공간이다. */}
── lib/assessment.test.ts:100
     98|   });
     99| 
    100|   it('실수로 유형을 둘 적어도 한 덩어리로 묶이지 않고 각 유형에 집계된다', () => {
    101|     const multi: Exam = {
── lib/assessment.test.ts:123
    121|   });
    122| 
    123|   it('scoreOf: 배점을 안 적은 시험지는 한 문항 1점이라 득점률이 곧 정답률이다', () => {
    124|     const qs = [1, 2, 3].map((no) => ({ no, type: '밀도' }));
── lib/assessment.ts:50
     48|   /**
     49|    * 딸린 인쇄물. 값은 사이트의 papers/ 에 올려 둔 파일 이름이거나 'http…' 주소다.
     50|    * 짧은 문자열이라 기존 동기화에 그대로 얹힌다.
     51|    */
── lib/assessment.ts:93
     91|   no: number;
     92|   // 채점 당시의 값을 함께 적어둔다. 시험지를 나중에 고쳐도
     93|   // 이미 저장된 채점의 점수가 흔들리지 않는다.
     94|   earned: number; // 득점
── lib/assessment.ts:274
    272|   const idxFormat = header.indexOf('format');
    273|   const idxTitle = header.indexOf('title');
    274|   // 인쇄물은 시험지 한 장에 하나뿐이라 문항이 아니라 시험지에 붙는다.
    275|   const attachCols = ATTACH_KINDS.map((k) => [k, header.indexOf(k)] as [AttachKind, number]).filter(
── lib/papersync.ts:26
     24|       if (res.questions.length > 0) {
     25|         made = {
     26|           id: '', // 붙일 때 정한다 — 이미 있는 시험지면 그 id를 이어 쓴다
     27|           title: (res.title || file.replace(/_?시험지\.csv$/i, '').replace(/_/g, ' ')).trim(),
components/ExamFiles.tsx:6: * 시험지 한 줄의 인쇄물 칸 — 문제지 · 해설 · 출제표.
components/ExamManager.tsx:120:                        return n ? `${n}개` : '—';
components/GradeDialog.tsx:11: * 않아도 된다. 키보드로도 같다 — O·X·숫자·Enter.
components/GradeDialog.tsx:120:                placeholder="—"
components/Logo.tsx:13:const FILL = 0.9; // 가장자리 여백 — 반올림으로 1px 잘리는 것까지 방지
components/ManualPage.tsx:77:            <b>[＋ 학생 추가]</b> — 이름과 학년만 넣으면 됩니다. 나머지는 나중에 채워도 됩니다.
components/ManualPage.tsx:80:            <b>[CSV 가져오기]</b> — 여러 명을 한 번에 넣습니다. <b>[내려받기]</b>는 지금 등록된 학생을 같은 형식의
components/ManualPage.tsx:84:            <b>학생 정보</b> — 학교·연락처·학부모 연락처·형제 재원 여부·메모·목표 고등학교·과목별 진도를 적습니다.
components/ManualPage.tsx:89:            <b>응시 결과</b> — 이 학생이 본 시험과 점수가 한 줄씩 나옵니다. 줄 끝 ✕ 로 그 응시만 지웁니다. 시험지와
components/ManualPage.tsx:93:            <b>유형별 정답률</b> — 여러 번 응시했으면 <b>전부 합친</b> 결과입니다. 여기서는 레이더와 막대를 같이
components/ManualPage.tsx:103:          <b>학생 CSV</b> — 필요한 열은 <b>이름</b> 하나입니다. 학년은 없으면 비워 둡니다.
components/ManualPage.tsx:108:          CSV로 못 넣습니다 — 학생을 고른 뒤 [학생 정보]에서 적으세요.
components/ManualPage.tsx:352:            <b>[직접 입력]</b> — 한 문항을 크게 띄웁니다. O나 X를 누르면 바로 다음 문항으로 넘어가므로 화면을
components/ManualPage.tsx:353:            내리지 않아도 됩니다. 키보드로도 같습니다 — <code>O</code> <code>X</code>(한글 자판{' '}
components/ManualPage.tsx:358:            <b>아래 표</b> — 문항마다 O/X를 직접 누릅니다. 몇 개만 고칠 때 빠릅니다. 같은 버튼을 한 번 더 누르면
components/ManualPage.tsx:362:            <b>[채점 CSV 올리기]</b> — 다른 분이 채점해 왔을 때 씁니다. <b>[채점 CSV 내려받기]</b>로 빈 표를 받아
components/ManualPage.tsx:419:            <b>선생님 의견</b>은 비워두면 인쇄에서 빠집니다. 인쇄에는 <b>3줄까지</b> 나갑니다 — 더 쓰면 미리보기에서
components/StudentManager.tsx:221:                  <span className="rt">{r === null || r === undefined ? '—' : `${Math.round(r * 100)}%`}</span>
components/StudentManager.tsx:422:                          <td>{ex?.title ?? '—'}</td>
components/TypeRadar.tsx:32:  // 각 유형이 차지하는 각도를 문제 수에 비례하게 — 단, 균등 배치와 섞어(BLEND)
components/TypeReport.tsx:221:    // 좁은 배치의 1쪽은 여섯 덩이 — 레터헤드·제목·학생·유형·이동그룹·푸터. 사이는 다섯 칸.
components/TypeReport.tsx:306:                  <td>{ex?.title ?? '—'}</td>
components/TypeReport.tsx:406:              여기에 적은 내용이 아래 미리보기와 PDF에 그대로 들어갑니다. (저장되지 않는 임시 입력 — 학생을 바꾸면 비워집니다)
lib/assessment.test.ts:175:    // O는 'full' — 몇 점인지는 시험지의 배점이 정한다. 4번은 미입력이라 빠진다.
lib/assessment.ts:1:// src/lib/assessment.ts — 진단평가 데이터(학생·시험지·채점) + CSV 임포트 + 집계
lib/assessment.ts:8:  type: string; // 유형 — 무엇을 하다 막히는가 (행동영역)
lib/assessment.ts:13:  unit?: string; // 단원 — 무엇을 안 배웠는가
lib/assessment.ts:14:  level?: string; // 난이도 — 어디서 멈추는가 (표준·상·최상)
lib/assessment.ts:400:// 이름 기준 업서트(있으면 갱신, 없으면 추가) — 채점 결과 보존
lib/assessment.ts:544:    // 유형은 약한 것부터 — 리포트의 레이더·막대가 이 순서를 그대로 쓴다
lib/assessment.ts:553://   유형   무엇을 하다 막히는가   — 약한 순
lib/assessment.ts:554://   단원   무엇을 안 배웠는가     — 시험지에 나온 순(교육과정 순)
lib/assessment.ts:555://   난이도 어디서 멈추는가        — 표준 → 상 → 최상
lib/assessment.ts:603:/** 그 축을 쓸 수 있는 시험지인지 — 단원·난이도를 안 적었으면 탭을 숨긴다. */
lib/assessment.ts:609:// 홈 화면의 '분석 유형' 숫자 — 유형 분류를 바꿔도 이 값이 저절로 따라온다.
lib/brand.ts:1:// src/lib/brand.ts — 업로드된 로고를 자동으로 사용
lib/cloud.ts:1:// src/lib/cloud.ts — 구글 로그인 + Firestore 문서 실시간 동기화 훅
lib/firebase.ts:1:// src/lib/firebase.ts — Firebase 초기화(구글 로그인 + Firestore 실시간 동기화)
lib/firebase.ts:6:// 프로젝트: student-assessment-analysis (이 앱 전용 — 시간표 앱의 sodam-alpha와 별개)
lib/papersync.ts:6:// 앱의 시험지 목록에 나타난다. 앱에서 올리는 길은 없다 — 시험지가 늘 저장소와
lib/papersync.ts:26:          id: '', // 붙일 때 정한다 — 이미 있는 시험지면 그 id를 이어 쓴다
lib/papersync.ts:52: * - 저장소에서 빠진 시험지는 지우지 않는다 — 채점 결과가 딸려 있을 수 있다
styles.css:1:/* styles.css — 진단평가 분석 디자인 시스템
styles.css:24:  /* 상태 — 색만으로 뜻을 전하지 않는다. 반드시 글자 라벨과 함께 쓸 것 */
styles.css:207:/* 시험지 목록의 인쇄물 칸 — 붙어 있으면 내려받기, 없으면 올리기. */
styles.css:366:/* 서술형 — 색만으로 구분하지 않도록 '서술형' 글자 배지를 함께 단다 */
styles.css:699:/* 집계 축 전환 — 유형 / 단원 / 난이도 */
styles.css:753:/* 20[   ]년[   ]월[   ]일 — 숫자를 적을 빈 칸. */
styles.css:868:/* 리포트 — 난이도는 섹션 제목 줄에 넣어 세로 공간을 쓰지 않는다 */
